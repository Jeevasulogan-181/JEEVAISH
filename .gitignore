# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
*.tsbuildinfo

# Local app data — this is a fully local app; none of this should ever
# be committed. Run `npm run reset` to wipe it.
/data/db.json
/data/*.corrupt-*.bak
/public/uploads/*/*
!/public/uploads/*/.gitkeep